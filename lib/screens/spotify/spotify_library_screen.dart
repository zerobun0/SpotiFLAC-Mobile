import 'package:flutter/material.dart';

/// Stub for Task 6 so the login screen compiles and can be verified in
/// isolation. Task 9 replaces this file's contents entirely with the real
/// Spotify library browsing UI.
class SpotifyLibraryScreen extends StatelessWidget {
  const SpotifyLibraryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Your Spotify Library')),
      body: const Center(child: Text('Coming soon')),
    );
  }
}
